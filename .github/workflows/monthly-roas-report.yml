name: Monthly ROAS Report

on:
  schedule:
    # 매월 1일 오전 9시 KST (UTC 0시)
    - cron: '0 0 1 * *'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  monthly-roas-report:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python dependencies
        run: pip install pandas numpy certifi

      - name: Install Node dependencies
        run: npm install

      - name: Create .env from secrets
        run: |
          cat > .env << 'ENVEOF'
          META_APP_ID="${{ secrets.META_APP_ID }}"
          META_APP_SECRET="${{ secrets.META_APP_SECRET }}"
          META_ACCESS_TOKEN="${{ secrets.META_ACCESS_TOKEN }}"
          NOTION_TOKEN="${{ secrets.NOTION_TOKEN }}"
          NOTION_PAGE_ID_ROAS="${{ secrets.NOTION_PAGE_ID_ROAS }}"
          NOTION_PAGE_ID_MARKETING="${{ secrets.NOTION_PAGE_ID_MARKETING }}"
          ENVEOF

      - name: Run Monthly ROAS Analysis (30 days)
        run: python3 scripts/daily_roas_analysis.py --days 30 --upload --period "월간"

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: roas-monthly-report-${{ github.run_number }}
          path: output/meta-ad/_roas_monthly/*.md
          retention-days: 365
